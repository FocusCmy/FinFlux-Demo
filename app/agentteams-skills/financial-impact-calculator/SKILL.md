---
name: financial-impact-calculator
version: 1.0.0
description: Compute deterministic financial impact from contract-approved fields and explicit formulas. Emit inputs, outputs, and hashes.
---

# financial-impact-calculator

Compute deterministic financial impact from contract-approved fields and explicit formulas. Emit inputs, outputs, and hashes.

The runtime must verify this instruction digest and the package entrypoint
digest before execution. A missing, mismatched, or tampered manifest is
fail-closed and must not produce a success receipt.
