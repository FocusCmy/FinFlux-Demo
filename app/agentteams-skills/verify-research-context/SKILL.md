---
name: verify-research-context
description: Verify that run-level research context is present and hash-addressed while preserving the boundary between context and financial truth.
---

# Verify Research Context

Verify nonzero item count and 64-character manifest and bundle SHA256 handles. Output `VERIFIED_CONTEXT` or `NEEDS_EVIDENCE`. Always record `context_is_financial_truth=false`; research narratives may explain a Case but cannot override deterministic contracts or calculations.
