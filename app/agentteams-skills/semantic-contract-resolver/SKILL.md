---
name: semantic-contract-resolver
version: 1.1.0
description: Resolve a candidate field mapping against a versioned financial semantic contract without using model-generated numeric truth.
---

# semantic-contract-resolver

Resolve a candidate field mapping against a versioned financial semantic contract without using model-generated numeric truth.

The runtime must verify this instruction digest and the package entrypoint
digest before execution. A missing, mismatched, or tampered manifest is
fail-closed and must not produce a success receipt.
